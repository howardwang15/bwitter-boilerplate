import { OPEN_MODAL, CLOSE_MODAL } from '../actions/modal';

export default (state, action) => {
    switch (action.type) {
        // TODO: add switching state to open/close modal
        default:
            return { ...state };
    }
};
